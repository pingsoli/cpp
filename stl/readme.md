# STL library map

```
for_each

begining from heap operations.
make_heap
push_heap
pop_heap

talk about max-heap and min-heap
make heap be a array.
std::make_heap(begin(numbers), ends(numbers))
std::push_heap(begin(numbers), ends(numbers))
std::pop_heap(begin(numbers), ends(numbers))

sorting
sort_heap
partial_sort
nth_element
inplace_merge : merge two sorted collections

partitioning
we msut get partition_point

permutation
rotate
shuffle : make it unordered
reverse

stable_*
  stable_sort
  stable_partition

*_n
*_copy
*_if

is_*
  is_sorted
  is_partitioned
  is_heap

is_*_until
  is_sorted_until
  is_partitoned_until
  is_heap_until

queries
all_of
any_of
none_of

equal
lexicographical_compare
mismatch

find
adjacent_find
equal_range
lower_bound
upper_bound
binary_search

find_first_of
```
